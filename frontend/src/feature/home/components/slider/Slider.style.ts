import { makeStyles } from '@mui/styles';

export const useStyles = makeStyles(() => ({
  sliderMain: {
  },
  sliderItem: {
    width: '100%',
    height: 'auto'
  },
  imageSlider: {
    objectFit: 'contain',
    maxWidth: '100%',
    maxHeight: '100%',
    width: 'auto',
    height: 'auto'
  }
}));